# yuxi

Yuxi 核心后端 Python 包。

该目录用于将 `yuxi` 作为本地可构建、可安装的 Python package 提供给 `backend` 项目使用。包名沿用上游开源框架 Yuxi 的命名，其中既包含从框架继承的通用智能体与知识库能力，也包含 ResearchCompass 针对科研场景新增的 `research_*` / `academic_*` 服务、仓储与工具。
