# 2026-07-21

## ResearchCompass 参赛项目（第八届中国研究生人工智能创新大赛）
- 项目定位：AI+科研辅助，知识图谱+RAG 学术文献分析平台，脚手架 Yuxi（FastAPI+LangGraph+Vue3，MIT）。
- 用户决策（本日确认）：**现在不清理任何文档**（无论自己规划文档还是 Yuxi 框架文档），全部原地保留；等系统做完甚至商业化部署阶段再统一清理。
- 清理原则（用户偏好）：倾向"非破坏式清理"——单独产出干净的提交/发布包，只收"自己写的代码+新写文档+视频"，不删原目录；**LICENSE/NOTICE/版权声明绝不能随文档一起删**（MIT 法律要件，商业化也要带）。
- 可选动作：我提议起草《终稿清理/发布包 checklist》（哪些进提交包、哪些留原目录、LICENSE 必带），用户尚未确认是否要现在写。
- 关键约束（来自赛事规则）：提交材料不得含学校/学院/导师信息；作品核心创意须大赛期间独立完成——用 MIT 框架作底座合法，但需在文档中诚实界定"底座 vs 自研增量"（已用创新点1-3 钉死）。

## Docker 环境搭建（本日晚间）
- 执行 `docker compose up -d` 首次拉起 Yuxi 开发栈。
- **根因阻塞**：当前机器网络 `docker.io:443` 不通（被墙/限），但 `quay.io`、`pypi.org` 通；未配置镜像加速器 → `milvus`/`minio`(在 docker.io)拉取必败，栈起不来。
- **可用镜像源（443 实测可达）**：`docker.m.daocloud.io`（道云）、`docker.xuanyuan.me`（轩辕）；`hub-mirror.c.163.com`/`registry.docker-cn.com`/`mirror.baidubce.com` 不通。
- **修复方案**：Docker Desktop → Settings → Docker Engine 加 `"registry-mirrors":["https://docker.m.daocloud.io"]` → Apply & Restart，然后重跑 `up -d`。
- 本地已存在 `yuxi-api/web/sandbox-provisioner:0.7.1.beta1` 镜像，配好镜像源后可设 `YUXI_VERSION=0.7.1.beta1` 复用，跳过 api 漫长本地构建。
- 用户已在 GUI 配好道云镜像源（Apply & Restart）。注意：用户 7/23 搬家换网络，该问题可能在新网络复现，需重新验证镜像源可达性。

## Docker 环境最终拉起成功（本日晚，清理重来后）
- **必须显式带 `YUXI_VERSION=0.7.1.beta1` 跑 `up`**：`.env` 未设该变量，compose 默认用 `0.7.1`，本地没有就拉/构建（慢且易败）。本地已有完整 `yuxi-*:0.7.1.beta1` 镜像，设该变量可跳过构建直接复用。
- **清理正确姿势**：`docker compose down -v`（删容器+数据卷+网络）。曾有 10 天前僵尸容器导致 `up` 同名冲突，需先 `docker rm -f` 残留再 up。
- **sandbox-provisioner 镜像坑（已解决）**：`yuxi-sandbox-provisioner:0.7.1.beta1` 本地镜像本身有缺陷——缺 `httpx` 模块且曾被误烤 `--entrypoint sh`（导致 `sh sh -lc` 报 "cannot open sh"）。最终修法：用本地正常的 `yuxi-sandbox-provisioner:0.7.1` 镜像 `docker tag ...:0.7.1 ...:0.7.1.beta1` 覆盖坏标签，再 `up -d --force-recreate sandbox-provisioner` 即 healthy。
- **最终全栈 healthy**：api-dev(5051) / web-dev(5173) / worker-dev / graph(Neo4j 7474/7687) / milvus(19530) / etcd / minio(9000-9001) / postgres(5432) / redis(6379) / sandbox-provisioner(8002)。api 健康检查路径是 `/api/system/health`（不是 `/health`）。
- 结论：环境已就绪，可开发。前端 http://localhost:5173，后端 http://localhost:5051。
