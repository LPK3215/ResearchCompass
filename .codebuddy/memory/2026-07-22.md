# 2026-07-22 日志

## Git 仓库绑定
- 用 gh（账号 LPK3215）为 ResearchCompass 项目创建了 GitHub 私密仓库：`https://github.com/LPK3215/ResearchCompass`。
- 本地 origin 已由旧的 `SourceCode-true.git` 改为指向 `https://github.com/LPK3215/ResearchCompass.git`（用户选择"替换 origin"）。
- main 分支 10 个提交，工作区干净，尚未推送（用户网络卡，决定本地操作、暂不推云端）。
- 坑：git 全局配了失效代理 `http.proxy/https.proxy=http://127.0.0.1:7897`，导致 push 报 `TLS ... unexpected eof`（但 gh api 直连正常）。推送前需临时 `git -c http.proxy= -c https.proxy= push` 或 unset 该代理。
