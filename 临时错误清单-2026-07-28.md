# ResearchCompass 错误清单（2026-07-28）

> 最后复核：2026-07-29（Asia/Shanghai）
>
> 性质：源码审计 + 修复闭环 + Docker Compose 真实运行 + 全量测试/Lint/构建/依赖扫描
>
> 范围：后端、前端、科研业务服务、知识库、Agent、PostgreSQL/Neo4j/Milvus/MinIO、LITE_MODE 与测试基础设施
>
> 修复状态：C-01～C-06、T-01～T-05、R-01～R-03、R-05～R-07、R-10 已完成；R-04 已确认合法语义；R-08 已部分修复（Neo4j 噪声已降噪，MinIO 待处理）；R-09 作为非阻断容量风险保留；E-07 依赖漏洞从 34 项降至 3 项。
>
> 安全约束：所有账号、密码、API Key、Token、运行 ID 均未写入本文。

## 一、结论摘要

本轮已修复全部 **6 个已确认产品错误**、**5 个测试基础设施/过期契约问题**和 **Redis/worker watcher 稳定性阻断（R-10）**，并处理了有明确最小方案的安全依赖和质量门禁问题。依赖漏洞升级将 pip-audit 从 34 项降至 3 项剩余（nltk 无修复版本、setuptools 为构建时问题），PyMilvus ORM API 迁移到 MilvusClient（R-07），Neo4j 空状态告警降噪（R-08 部分）。默认 Compose 与从零重建均正常；后端 unit 1505 passed、integration 187 passed、E2E 11 passed；前端测试、Lint、构建和生产依赖扫描均通过；真实用户主链路浏览器走查 8/8 通过。剩余 R-08 的 MinIO 噪声和 R-09 容量压测作为非阻断技术债保留。

本清单不再沿用旧稿的“看到可疑代码即算错误”口径。条目分为：

- **已确认错误**：有真实请求/日志/确定性纯运行复现，或源码路径可以无条件证明。
- **风险与技术债**：代码存在明确薄弱点，但当前环境未复现用户可见故障。
- **测试缺陷**：失败来自测试运行方式或过期契约，不能误报为生产功能错误。
- **已排除疑点**：审计后确认不是错误，防止修复时误改正确设计。

优先级定义：

- **P0**：当前阻断核心系统或存在已证实的直接数据破坏；本次未发现。
- **P1**：可能造成生产 500、连接池耗尽、敏感信息落日志或默认核心功能确定性失败，应优先处理。
- **P2**：功能契约不一致、稳定性边界或高价值测试基础设施问题。
- **P3**：升级、可观测性、规模风险或低优先级质量门禁。

## 二、已确认的产品错误

### C-01 [P1/已修复] SSE 请求取消时 PostgreSQL 会话可能无法归还连接池

- **位置**：
  - `backend/package/yuxi/storage/postgres/manager.py:1043-1056`
  - `backend/server/utils/auth_middleware.py:18-20`
  - `backend/server/routers/agent_router.py:482-495`
- **修复前真实证据**：结束/取消真实 Agent SSE 请求后，API 日志出现：
  - `Exception terminating connection`
  - `asyncio.exceptions.CancelledError`
  - `The garbage collector is trying to clean up non-checked-in connection`
- **根因**：`get_async_session_context()` 只捕获 `Exception`；在当前 Python 中 `asyncio.CancelledError` 不会进入该回滚分支。随后 `finally` 中的 `await session.close()` 仍处在取消作用域，也可能再次被取消。认证依赖持有请求级数据库会话，而 `StreamingResponse` 会把依赖生命周期延长到流结束，使正常断流也能触发这条路径。
- **影响**：客户端刷新、网络断开、主动取消长时间 Agent 请求时，连接可能不归还；持续运行后可能逐渐耗尽 SQLAlchemy 连接池，导致无关 API 超时或失败。
- **修复要求**：为取消路径提供可靠的 rollback/close，并保证清理阶段不被同一次取消打断；不要吞掉取消信号。
- **修复结果**：显式捕获 `asyncio.CancelledError`，使用受屏蔽的 rollback/close 完成清理后继续传播取消；单元测试覆盖取消、业务异常和正常关闭。真实 Agent SSE 强制断流后取消与清理均成功，近 30 分钟 API 日志中的连接泄漏、跨 loop 和事件循环关闭特征均为 0。
- **验收**：
  - [x] 真实建立并主动断开 SSE，随后取消任务、清理线程成功。
  - [x] 日志不再出现 non-checked-in connection 或 GC 回收连接警告。
  - [x] 正常完成、业务异常、客户端取消三条路径均有回归覆盖，取消继续向上传播。

### C-02 [P1/安全与隐私/已修复] 请求校验异常的原始输入会完整写入 ERROR 日志

- **位置**：`backend/package/yuxi/storage/postgres/manager.py:31-38`
- **修复前真实证据**：发送一个会触发 422 的真实请求后，数据库会话层 ERROR 日志包含 Pydantic 校验错误文本，其中带有原始 `input` 字段。
- **根因**：`_log_async_session_rollback()` 只根据异常对象的 `status_code` 降级 HTTP 4xx；对 `RequestValidationError`/Pydantic `ValidationError` 直接执行 `logger.error(... {error})`。这类异常的字符串表示会包含用户原始输入。
- **影响**：密码、Token、外部知识库凭据或科研敏感文本一旦校验失败，可能进入集中日志、备份或日志平台；同时正常 422 被误记为 ERROR。
- **修复要求**：识别请求/Pydantic 校验异常，只记录异常类型、状态、字段位置和不含输入值的摘要；不要依靠关键词替换做不完整脱敏。
- **修复结果**：`RequestValidationError`/Pydantic `ValidationError` 仅记录异常类型和错误数量；4xx 与未知异常均不再拼接异常原文。真实 422 请求使用运行时生成的唯一哨兵验证，日志中未出现该值。
- **验收**：
  - [x] 唯一哨兵值触发真实 422 后，API 日志中搜索不到哨兵值。
  - [x] 422 作为预期校验失败处理，不再输出包含请求输入的 PostgreSQL ERROR。
  - [x] 非预期异常仍记录异常类型，并保留异常日志调用的诊断信息。

### C-03 [P1/已修复] 外部知识库打开/查找不存在文件错误返回 500

- **位置**：
  - `backend/package/yuxi/knowledge/base.py:862-898`
  - `backend/server/routers/external_kb_router.py:97-141`
- **修复前真实复现**：对真实外部知识库调用：
  - `GET .../files/file_does_not_exist/open`
  - `POST .../files/file_does_not_exist/find`
  - 修复前均返回 500；路由已有的客户端错误契约期望捕获 `ValueError` 并返回 400。
- **根因**：基础知识库的 `open_file_content()`/`find_file_content()` 捕获 `_load_file_meta()` 的 `ValueError` 后又改包成普通 `Exception`；路由只将 `ValueError` 映射为 400，普通异常统一变成 500。文件夹和“没有解析后 Markdown”也使用普通 `Exception`，存在同类问题。
- **影响**：Agent 或用户传错文件 ID 时被误报服务端故障；客户端无法区分可修正输入与系统异常，监控产生伪 500。
- **修复要求**：统一知识库文档访问的领域异常，并明确“不存在/是文件夹/尚无 Markdown”分别对应的稳定 4xx。若选择 404 而非现有 400，也应同步 API 契约和调用方。
- **修复结果**：不存在、文件夹和无 Markdown 均保留为 `ValueError`，沿用既有路由契约稳定映射为 HTTP 400；真实外部知识库 open/find 不存在文件集成用例通过。
- **验收**：
  - [x] open/find 的不存在文件均返回约定 400，不返回 500。
  - [x] 文件夹、未解析/无 Markdown 与真实存储异常使用不同异常边界。
  - [x] 内外部知识库继续复用同一基础访问语义。

### C-04 [P1/已修复] 知识库类型参数校验错误被统一转换为 500

- **位置**：
  - `backend/server/routers/knowledge_router.py:245-313,432-476`
  - `backend/package/yuxi/knowledge/implementations/dify.py:55-67`
  - `backend/package/yuxi/knowledge/implementations/notion.py:222-238`
- **修复前真实复现**：创建 Dify 知识库时缺少必填参数，或 `dify_api_url` 不以 `/v1` 结尾，修复前返回 500；这些都是客户端可修正的配置错误，应为稳定 4xx。
- **根因**：各知识库类的 `normalize_additional_params()`/`validate_additional_params()` 合理地抛出 `ValueError`，但 create/update 路由的通用异常边界将其统一改成 500。Notion 缺 `notion_data_source_id` 或 token 走同一条错误路径，因此不能只针对 Dify 打补丁。
- **影响**：前端无法向用户显示具体字段错误，监控误报服务故障；所有新增知识库类型都容易重复该问题。
- **修复要求**：在 create/update 的共同边界将类型参数校验异常映射为不泄密的 400/422，同时保留真正初始化故障的 500。
- **修复结果**：知识库 create/update 共同捕获 `ValueError` 并返回 400；集成测试覆盖 Dify 缺参/错误 URL、Notion 缺 data source，以及 Dify 更新错误 URL，响应不回显凭据值。
- **验收**：
  - [x] Dify 缺字段、URL 格式错误在创建和更新时均返回 400。
  - [x] Notion 缺 data source 等类型参数错误返回 400。
  - [x] 响应可定位配置问题且不回显 token 值。
  - [x] 仅收窄 `ValueError` 边界，真正初始化/连接异常仍为服务端错误。

### C-05 [P1/已修复] 默认严格图谱检索可确定性触发 `KeyError` 并返回 500

- **位置**：
  - `backend/package/yuxi/services/research_search_service.py:270-299`
  - `backend/package/yuxi/knowledge/graphs/academic_graph_service.py:256-319`
- **修复前确定性复现**：直接运行现有 PPR 排名逻辑，构造多个图节点并让某个检索种子的 PPR 排名低于 `top_k`。结果中 `papers` 只保留截断后的节点，而 `paper_scores` 仍包含全部节点；低排名种子因此不在返回的 `papers` 中。
- **根因**：
  1. `rank_papers_by_ppr()` 返回 `papers = ranked[:top_k]`，但 `paper_scores` 返回全部可达节点。
  2. `_apply_citation_graph()` 只用截断后的 `papers` 查询 library paper 映射。
  3. 随后对每个原始文本检索结果直接执行 `graph_id_by_paper_id[str(item["paper_id"])]`。
  4. 任一文本种子未进入 PPR 前 `top_k` 即 `KeyError`，严格模式整体失败。
- **影响**：这是默认严格图谱检索主链路；结果数、`top_k` 和图分数满足常见条件时即可 500，并非只在脏数据下发生。
- **修复要求**：原始检索种子的映射与 PPR 扩展结果截断必须解耦；所有参与融合的文本结果必须有明确 graph ID/score，或在进入融合前以领域错误显式拒绝。仅把字典下标改成 `.get()` 并静默跳过会改变严格模式语义，不算完整修复。
- **修复结果**：查询 library paper 映射时始终包含全部原始 `seed_weights`，再追加截断后的扩展论文；低排名种子即使未进入 PPR `papers[:top_k]` 也仍能取得 graph ID 并参与融合，没有静默跳过。
- **验收**：
  - [x] 确定性构造低排名种子未进入 PPR 前 `top_k`，严格检索仍完成融合且不抛 `KeyError`。
  - [x] 原始结果均保留 graph/fusion score。
  - [x] 现有多个种子、排序和不可达节点覆盖继续通过。

### C-06 [P2/已修复] 子智能体“默认审批”文案与实际能力相反

- **位置**：
  - `backend/package/yuxi/agents/context.py:183-193`
  - `backend/package/yuxi/agents/buildin/subagent/graph.py:31-73`
  - `backend/package/yuxi/repositories/agent_repository.py:288-297`
- **修复前真实证据**：真实 SubAgent 文件写入 E2E 中，父运行最终为 `interrupted`；子智能体在默认模式下无法调用写文件工具。
- **根因**：配置文案称默认模式会在写文件、编辑文件、执行命令前“请求确认”；实际实现因子智能体无法弹出主线程逐项审批，直接从模型工具列表隐藏敏感工具，并在调用边界抛 `PermissionError`。内置 general-purpose 子智能体又使用空配置，默认落入该模式。
- **影响**：用户按 UI/描述预期会看到确认提示，实际上文件处理任务直接失败；内置子智能体的“文件处理”能力在默认配置下不可用。
- **产品决策**：本轮采用安全且最小的既有语义——主 Agent 默认请求确认，SubAgent 默认禁用敏感工具；只有完全信任模式允许子智能体文件处理。本轮不新增跨智能体审批协议。
- **修复结果**：上下文配置文案、内置 general-purpose SubAgent 描述和测试契约已统一；仅在数据库描述仍等于旧内置默认值时迁移，避免覆盖管理员自定义说明；真实写文件 E2E 显式配置 `always_trust` 并通过。
- **验收**：
  - [x] 配置文案、内置说明、SubAgent 工具列表和测试使用同一审批语义。
  - [x] 默认模式明确说明敏感工具不可用，不再承诺子线程内弹出确认。
  - [x] `always_trust` 下真实写文件任务成功，默认模式工具过滤有独立覆盖。

## 三、测试基础设施与过期测试契约

> 本节问题会让测试失败，但不能直接等价为对应生产功能损坏。应先修测试基础设施，再以干净结果判断剩余产品回归。

### T-01 [P1/测试基础设施/已修复] pytest 异步事件循环生命周期自相矛盾

- **位置**：
  - `backend/pyproject.toml:64-65`
  - `backend/test/integration/conftest.py:37-50`
- **修复前现状**：配置同时指定 `asyncio_default_fixture_loop_scope = "function"` 与 `asyncio_default_test_loop_scope = "session"`；session 级同步 fixture 又通过 `anyio.run()` 在额外事件循环中初始化全局 `pg_manager` 连接池。
- **修复前真实结果**：默认集成测试为 **99 passed、85 failed、4 skipped**，反复出现 `Future attached to a different loop` 和 `Event loop is closed`。临时统一 loop scope 后主体明显恢复，但全局连接池仍可能由另一个 loop 创建，说明不能只改一行 pytest 配置。
- **影响**：大量失败是级联噪声；Research Copilot E2E 也因跨事件循环数据库连接失败。当前“85 failed”不能解释成 85 个生产 Bug。
- **修复要求**：让测试、异步 fixture、全局 PostgreSQL 引擎/连接池的创建和销毁处在明确且一致的事件循环生命周期；结束时显式 dispose。
- **修复结果**：fixture/test loop scope 统一为 session；全局 `pg_manager` 在同一 session loop 初始化并在结束时关闭；移除 evaluation router 最后一处局部 function loop 覆盖。完整 integration 结果为 **187 passed、4 skipped**，没有 different loop 或 loop closed。
- **验收**：
  - [x] 全量集成测试不再出现 different loop / loop closed。
  - [x] 定向、单文件和全量集成均通过，共享 fixture 可正常关闭。
  - [x] Research Copilot E2E 不再因数据库 loop 归属失败。

### T-02 [P2/已修复] 文档入队测试仍用空 `items` 期待 200

- **位置**：`backend/test/integration/api/test_task_router.py:50-92`
- **修复前问题**：测试名为“创建任务”，却发送 `items: []` 并期待 200；生产接口已明确将空列表视为 400。当时测试没有创建真实上传项，契约过期。
- **修复要求**：若要验证入队成功，fixture 先上传真实文件并传入有效 item；另保留一个独立测试验证空列表 400。
- **修复结果**：集成 fixture 真实上传文本、真实入队并轮询任务成功；成功路径传有效 `file_id`，空 items 独立断言 400。

### T-03 [P2/已修复] Viewer 图片/PDF 集成测试仍把二进制响应当 JSON

- **位置**：
  - `backend/test/integration/api/test_viewer_filesystem_router.py:261-306`
  - `backend/package/yuxi/services/viewer_filesystem_service.py:146-162`
- **修复前问题**：生产接口和前端已按 Blob/二进制流消费，并通过 `X-Yuxi-Preview-*` 响应头提供元数据；两个测试仍调用 `response.json()` 并断言旧 JSON 字段。
- **修复要求**：断言 Content-Type、响应体字节、预览响应头和下载/内联语义，不应把生产接口改回旧 JSON 来迎合测试。
- **修复结果**：图片/PDF 测试改为验证二进制响应、Content-Type 与 `X-Yuxi-Preview-*` 元数据头，生产接口未回退到旧 JSON 契约。

### T-04 [P2/已修复] SubAgent 写文件 E2E 未显式设置 `always_trust`

- **位置**：`backend/test/e2e/test_subagent_stream_e2e.py:182-225` 及其创建 Agent 配置
- **修复前问题**：测试强制子智能体写文件，却沿用默认审批模式。按实现语义敏感工具会被隐藏，所以失败与 C-06 的产品契约有关，但测试本身也没有声明所需授权。
- **修复要求**：写入成功用例显式设置 `tool_approval_mode="always_trust"`；另建/保留默认模式拒绝用例，避免一个测试同时承载两种相反语义。
- **修复结果**：真实文件写入 E2E 显式设置 `always_trust` 并通过；默认模式由现有工具过滤单元测试验证敏感工具不可用。

### T-05 [P2/已修复] 部分集成测试用无条件 skip 掩盖 fixture 缺失

- **位置示例**：`backend/test/integration/api/test_knowledge_router.py:490-503`
- **修复前问题**：思维导图测试验证空文件后直接 `pytest.skip("需要先上传文件...")`；另有若干 chat/viewer 测试因“没有 Agent/缺预置字段”跳过。根据 `docs/develop-guides/testing-guidelines.md`，可由本地 fixture 构造的前置条件不应 skip。
- **修复要求**：由 fixture 创建文件、Agent 和必要记录；只有外部模型、专用凭据或不可控第三方服务未配置时才允许 skip。
- **修复结果**：新增真实 `knowledge_document` fixture；思维导图使用真实文件与当前真实模型完成生成、保存和读取；chat/viewer 缺内置 Agent 改为显式失败。剩余 skip 仅为未配置的外部多模态/专用模型场景。

## 四、风险、依赖与技术债

### R-01 [P2/已修复] Semantic Scholar 对瞬时故障的重试策略不足

- **位置**：`backend/package/yuxi/services/semantic_scholar_service.py:121-157`
- **修复前现状**：只重试 429；5xx、超时和瞬时网络错误立即失败。最小请求间隔仅在 `attempt == 0` 执行，重试前也没有更新 `_last_request_time`。
- **风险**：单次第三方抖动可中断整轮学术图谱同步，连续重试的本地节流状态也不完整。
- **已验证边界**：本次真实 Semantic Scholar DOI 请求成功，因此这不是“当前 API 不通”，而是稳定性风险。
- **建议验收**：用可控 stub/代理验证 429、500/502/503、超时、Retry-After 和最终失败；限制总重试时长并保留可观测错误码。
- **修复结果**：5xx、超时和 `httpx.HTTPError` 采用有限 3 次退避重试，每次真实请求更新节流时间；保留 429 `Retry-After` 语义。503、超时、网络错误及既有 429 回归均通过。

### R-02 [P2/已修复] 历史或异常用户研究 JSON 缺键会使报告/导出 `KeyError`

- **位置**：`backend/package/yuxi/services/research_user_study_service.py:105-122`
- **修复前现状**：`_sus_score()` 和 `_summary()` 对固定题目键直接下标访问。正常提交入口会验证字段完整，所以新数据主路径正常；历史迁移、人工修改或旧 schema 记录缺键时，整份报告/CSV 可能失败。
- **建议**：先明确历史数据策略。可选择迁移并拒绝异常记录，或导出时报告具体坏记录；不建议静默填 0，因为会扭曲研究统计。
- **修复结果**：报告与 CSV 导出先校验历史 score map；缺键或非法分数统一抛出 `response_data_invalid`（“用户评测历史响应数据不完整”），路由显式返回 500，不再泄漏 `KeyError` 或静默填 0 扭曲统计。

### R-03 [P2/已修复] LITE_MODE 的科研后台恢复边界不完整

- **位置**：`backend/server/utils/lifespan.py:165-221`
- **修复前现状**：LITE_MODE 会正确跳过科研/知识库路由和主要初始化，但 lifespan 仍无条件导入并执行论文重建索引、图谱同步、论文分析、综述、外部导入和知识库评估恢复函数。
- **已验证边界**：一次性真实 LITE_MODE 容器启动成功，health 为 200，OpenAPI 中 research/knowledge 路由均为 0；当前数据库无 pending 数据时不阻断启动。
- **风险**：若库内残留 pending 任务，精简模式仍可能访问本应禁用的科研依赖、制造错误日志或执行后台任务。
- **建议验收**：准备 pending 记录启动 LITE_MODE，确认不会启动科研 worker/访问 Neo4j、Milvus、MinIO；普通模式恢复行为保持不变。
- **修复结果**：所有科研恢复逻辑集中到 `_recover_research_tasks()`，LITE_MODE 在导入任何科研恢复模块前直接返回。单元测试验证禁用分支不会导入/调用恢复实现；临时真实 LITE_MODE 实例 health/OpenAPI 均为 200，日志确认跳过恢复且恢复执行记录为 0。

### R-04 [已确认合法语义] 图谱同步允许引用和参考文献上限为 0

- **位置**：`backend/server/routers/research_router.py:433-436`
- **现状**：`citation_limit`/`reference_limit` 使用 `ge=0`；下游对 0 返回空边，同步仍可成功并生成只有论文节点、没有引用关系的“引用图谱”。
- **产品结论**：保留 0 作为“仅同步论文节点、不展开引用/参考文献边”的合法能力。API 参数描述已明确该语义，不修改既有校验和下游行为。

### R-05 [P1/依赖安全/已修复] 前端生产依赖扫描发现 6 项 advisory

- **修复前扫描结果**：`pnpm audit --prod` 报告 **3 low / 1 moderate / 2 high**。
- **涉及版本**：
  - `undici 6.24.0`（经 `markmap-lib > markmap-html-parser > cheerio > undici`）：1 high、1 moderate、2 low；其中高危 `GHSA-vxpw-j846-p89q` 为 WebSocket 分片数量导致的 DoS，修复版本 `>=6.27.0`。
  - `postcss 8.5.16`：1 high，`GHSA-r28c-9q8g-f849`，`sourceMappingURL` 路径穿越读取 `.map`，修复版本 `>=8.5.18`。
  - `dompurify 3.4.11`：1 low，修复版本 `>=3.4.12`。
- **可达性判断**：
  - 当前前端代码未直接使用该传递 `undici` 的 WebSocket API。
  - PostCSS 主要位于构建链，未发现服务端处理用户不可信 CSS 的路径。
  - 当前 DOMPurify 用法未发现 advisory 所需的 `CUSTOM_ELEMENT_HANDLING`/相关 hooks。
  - 因而扫描严重度高，但本次未证明存在可直接利用的线上攻击路径；仍应升级锁文件并复测 markmap/Markdown/构建。
- **验收**：`pnpm audit --prod` 对上述 advisory 清零，前端测试和生产构建通过，思维导图/Markdown 渲染无回归。
- **修复结果**：DOMPurify 升级至 3.4.12，并通过 overrides 将 PostCSS 固定到 8.5.24、Undici 固定到 6.28.0；`pnpm audit --prod` 为 0，74 项前端测试、ESLint 和生产构建通过，真实思维导图集成通过。

### R-06 [P3/已修复] 后端 Ruff 质量门禁有 1 项失败

- **位置**：`backend/server/main.py:80`
- **修复前结果**：`E501 Line too long (145 > 120)`。
- **影响**：不影响运行，但会使要求 Ruff 全绿的提交/CI 失败。只需按当前文件风格拆分该描述字符串，不应顺带格式化无关代码。
- **修复结果**：按原文件结构拆分 FastAPI description 长行并完成格式收敛；全仓 `ruff check .` 与 `ruff format --check .` 均通过，478 个 Python 文件无格式差异。

### R-07 [P3/已修复] 多项依赖进入弃用/迁移窗口

后端/E2E warning（不影响当前功能，需在上游移除前迁移）：

- SQLAlchemy `declarative_base()` 旧入口警告。
- `langchain-community` 相关能力提示后续 sunset。
- PyMilvus ORM API 弃用（pymilvus 3.0.0 仍支持，3.1 移除）：
  - `connections.connect`（`backend/package/yuxi/knowledge/implementations/milvus.py:325`）
  - `db.list_database`（`milvus.py:328`）
  - `db.using_database`（`milvus.py:330`）
  - `connections.disconnect`（`milvus.py:1502`）
  - 迁移方向：改用 `MilvusClient` API。
- LangGraph v3 stream protocol 仍标记 beta（`test_streaming_toolcall_fix.py` 触发 `LangChainBetaWarning`）。
- Starlette TestClient/httpx 弃用提示（仅影响测试，建议升级到 `httpx2`）。
- SubAgent context 的 Pydantic serializer warning（序列化 SubAgent 运行上下文时出现，不影响功能，属于序列化字段类型提示）。

前端 warning（不影响功能）：

- Node mock 的 `ExperimentalWarning`（Node.js 实验性 API，测试工具链产生）。
- `@vueuse/core` / Rolldown 的 `INVALID_ANNOTATION` 提示（构建工具注解警告，第三方库产生）。

**修复结果：** PyMilvus ORM API 已全部迁移到 `MilvusClient`（见 changelog v0.11.0），`pymilvus<3.1` 约束确保在 ORM API 移除前完成迁移，api-dev 日志无 `PyMilvusDeprecationWarning`。其余 warning 为上游第三方库产生，不影响当前功能，按上游稳定版本逐步处理。未使用全局 warning filter 伪造消失。

### R-08 [P3/部分修复] MinIO/Neo4j 的预期空状态产生较多告警噪声

- 测试或空图环境中，`public` bucket 不存在时知识库清理会记录 list 警告。
- 新知识库尚无图数据时，Neo4j 会输出 label/property 不存在的通知。
- 正常 422 被记为 PostgreSQL ERROR 的部分已单列为 C-02，因为它还涉及敏感输入。

**修复结果：** Neo4j 驱动配置新增 `notifications_min_severity='ERROR'`，过滤空图状态下 label/property key 不存在的 WARNING 通知，真实存储故障仍以 ERROR 可见。MinIO 的 `public` bucket 不存在时的 list 警告仍保留，因其属于真实存储状态提示，不应静默。

### R-09 [P3/需压测] 代码中存在若干规模性瓶颈，但本次未复现故障

- BibTeX 整库导出先把全部论文加载到内存再拼接；指定多个 paper ID 时逐条查询。
- 外部论文导入恢复在启动时可能按知识库扫描大量文件记录。
- 项目报告导出存在可避免的重复查询。
- 引用边按条 upsert，大图同步可能产生大量往返。

**状态：保留为非阻断容量风险。** 这些属于压测候选，不应在没有数据规模、耗时和内存证据时按功能 Bug 修复。建议先以典型库、上限库建立基线，再决定批处理/流式化范围。

### R-10 [P1/已修复] Redis 临时故障放大与 worker watcher 崩溃导致正在执行的任务被取消

- **位置**：
  - `backend/package/yuxi/config/cache.py`
  - `backend/package/yuxi/models/providers/cache.py`
  - `docker-compose.yml`（worker service command）
- **修复前真实证据**：
  - runtime config sync 线程每 5 秒重新解析 Docker Redis DNS，Redis 短暂不可用时模型列表变空或重复阻塞。
  - 第一轮完整 E2E 出现 `10 passed, 1 failed`，唯一失败是 PDF OCR Agent run 被标记为 cancelled。日志根因是 `WatchfilesRustInternalError: Cannot allocate memory (os error 12)`，worker watcher 崩溃后发送 SIGINT，正进行的 PDF OCR Agent run 被取消。
- **根因**：
  1. runtime config sync 线程没有复用 Redis 客户端，每轮重新解析 DNS；Redis 临时不可用时本地模型快照未被保留。
  2. worker 由外层 `watchfiles` 包装运行，Docker Desktop on Windows 的文件系统通知机制下偶尔触发 `Cannot allocate memory`，watcher 崩溃后向 worker 进程发送 SIGINT，正在执行的长任务被取消。
- **修复结果**：
  - Redis：runtime config sync 线程复用一个 Redis 客户端，不再每 5 秒重新解析 Docker Redis DNS；Redis 临时不可用时继续使用已有本地模型快照；缓存重建后保留本地快照，避免 Redis 短暂故障导致模型列表变空或重复阻塞。定向回归 20 passed。
  - worker：`docker-compose.yml` 中 worker command 改为 `uv run --no-sync --no-dev arq server.worker_main.WorkerSettings`，不再由外层 `watchfiles` 包装运行。API 仍保留 uvicorn reload。
- **取舍说明**：worker 现在不再自动热重载，代码变更后需要重启/redeploy worker。这是稳定性与开发热重载之间的明确取舍——原配置会在 Docker Desktop 中中断正在执行的长任务。
- **验收**：
  - [x] 第二轮完整 E2E：11 passed、0 skipped（PDF OCR 不再被 watcher 崩溃取消）。
  - [x] 从零 Compose 重建后 worker-dev restart=0，无 watcher 崩溃，Redis 无 DNS 错误。
  - [x] 模型缓存：siliconflow-cn provider 8 个模型，全局模型缓存 9 个模型，E2E 临时加入的视觉模型已通过 `finally` 精确清除无泄漏。

## 五、已排除疑点（不要按旧稿修复）

### E-01 线程产物路径穿越：已排除

- 路径通过虚拟路径解析、`.resolve()` 和 workspace/uploads/outputs 边界限制。
- 已有符号链接逃逸测试；真实 Viewer 集成/E2E 路径安全用例通过。
- 不应仅因为路由使用 `{path:path}` 就判定可读取任意文件。

### E-02 综述证据内容严格相等：属于完整性设计

- 综述使用证据快照来保证可复现和可审计；内容变化时拒绝继续是明确的完整性策略。
- 不应擅自改成空白归一化或只比 `chunk_id`，否则可能接受已被修改的证据。

### E-03 “重复 chunk_id 导致集合不等”：旧判断不成立

- 现有逻辑先按字典键去重，再比较集合；重复 ID 本身不会造成旧稿描述的失败。

### E-04 Cypher label 拼接注入：已排除

- 动态 label 经 `safe_neo4j_label` 处理；Neo4j label 也不能像普通属性值一样直接参数化。
- 当前没有证据表明存在可利用的 label 注入。

### E-05 `create_project_task` 返回 `milestone_not_found`：基本合理

- repository 返回 `None` 的显式条件是传入 milestone 不属于项目；此时“里程碑不存在”比“任务不存在”准确。
- 最多可在提示中附带 milestone 标识，不应将错误码改成 `task_not_found`。

### E-06 LITE_MODE 当前并非启动阻断

- 真实精简容器已启动成功，health 200，科研和知识库路由未注册。
- lifespan 恢复任务边界已按 R-03 修复；再次启动临时真实实例确认不执行科研恢复逻辑。

### E-07 Python 依赖一致性当前通过，依赖漏洞扫描经升级后从 34 项降至 3 项

- `uv pip check`：305 个包兼容，无版本冲突。
- `pip-audit --local`（pip-audit 2.10.1，查询 OSV 数据库）：
  - 初始扫描发现 **34 项已知漏洞，涉及 9 个包**（pillow 12.2.0 13 项、pypdf 6.13.3 4 项、pyasn1 0.6.3 3 项、setuptools 81.0.0 2 项、json-repair 0.59.10、langsmith 0.8.7、mcp 1.27.2、nltk 3.9.4、pydantic-settings 2.14.1 各 1 项）。
  - 经依赖升级后降至 **3 项剩余**：`nltk 3.9.4`（路径遍历，暂无修复版本）、`setuptools 81.0.0`（2 项，构建时 Unicode 规范化绕过，修复 83.0.0）。
- 升级措施：
  - `pypdf` 约束从 `>=6.13.3` 升级到 `>=6.14.2`（修复 4 项 DoS/无限循环漏洞）。
  - 新增 `mcp>=1.28.1,<2.0.0` 约束（修复 WebSocket transport 缺 Host/Origin 校验漏洞，同时避免 mcp 2.0.0 的 `RequestContext` 破坏性 `ImportError`）。
  - 通过 `uv lock` 刷新拉取 pillow/pyasn1/json-repair/langsmith/pydantic-settings 等传递依赖的安全版本。
- 无法审计：`torch 2.12.1+cpu`、`torchvision 0.27.1+cpu`（CPU 版本不在 PyPI 标准索引）、`yuxi 0.11.0`（项目自身）。
- 剩余风险评估：nltk 路径遍历需控制输入文件路径，当前业务路径未直接暴露；setuptools 为构建时依赖，不影响运行时。
- **状态：依赖漏洞升级完成，剩余 3 项无可升级修复（nltk 无修复版本、setuptools 为构建时问题）。**

### E-08 风格/重构建议不计入错误

- 巨型 Vue 组件、魔法数字、单次 helper、轮询写法、Blob 返回风格、局部重复和未证明可达的死代码，不应混入功能错误清单。
- 若后续要重构，应单独立项并用行为测试保护，避免在本轮修复中扩大改动面。

## 六、实际执行结果

| 检查项 | 结果 | 结论 |
|---|---:|---|
| 默认 Docker Compose 服务 | API、worker、Web、PostgreSQL、Redis、Milvus、Neo4j、MinIO、sandbox-provisioner 均运行；除 worker 未定义 healthcheck 外其余正常 | 基础环境可用 |
| 从零 Compose 重建 | `down --remove-orphans` + `up -d --build` 成功；10 个服务全部启动，基础设施全部 healthy；api-dev 启动时 1 次 watchfiles 内存错误致重启，重启后稳定 healthy；worker-dev restart=0 | 通过（api 重启为 Docker Desktop 已知问题） |
| API health | HTTP 200 | 通过 |
| Web 首页 | HTTP 200 | 通过 |
| 修复后真实 LITE_MODE 启动 | health 200；OpenAPI 200；明确记录跳过科研恢复；恢复执行记录 0；临时容器已删除 | 通过 |
| 后端单元测试 | 1505 passed、1 skipped（MinerU OCR 服务未启动） | 通过 |
| 前端单元测试 | 74 passed | 通过 |
| 前端 ESLint | 通过 | 通过 |
| 前端生产构建 | 成功；仅第三方 `@vueuse/core` 注解警告 | 通过 |
| Ruff | 全仓 `ruff check .` 与 `ruff format --check .` 均通过；478 个 Python 文件无格式差异 | 通过 |
| 后端集成测试 | 187 passed、4 skipped（需 `RUN_MODEL_PROVIDER_CONNECTIVITY=1`） | 通过；无跨事件循环错误，skip 仅为外部依赖场景 |
| 真实 E2E | 11 passed、0 skipped | 通过（前序第二轮完整 E2E 结果，覆盖异步/同步 Agent、Agent Eval/Call、附件、视觉模型、PDF OCR、非视觉 OCR fallback、Research Copilot、SubAgent 文件产物、Viewer） |
| Python 依赖一致性 | 305 个包兼容 | 通过 |
| Python 漏洞扫描 | 经依赖升级后 **3 项剩余**（nltk 无修复版本、setuptools 构建时问题），从初始 34 项降至 3 项 | 通过（剩余项无可升级修复） |
| 前端生产依赖扫描 | 0 项已知漏洞 | 通过 |
| Compose 配置解析 | `docker compose config --quiet` 通过 | 通过 |
| Semantic Scholar 实际连通与重试 | DOI 论文请求成功；5xx、超时、网络错误与 429 回归通过 | 通过 |
| SSE 强制断流 | 请求建立后强制断开，取消 200、线程清理 200；连接泄漏/跨 loop 特征日志均为 0 | 通过 |
| 422 日志脱敏 | HTTP 422；运行时唯一哨兵未出现在 API 日志 | 通过 |
| 真实用户主链路走查 | 登录/ResearchCompass 首页/Agent 对话/附件上传/知识库入口/业务子页面/页面跳转 8/8 通过 | 通过；"用户评测"标签遮挡已修复（`.research-content` 新增 `overflow-x: hidden`）、路由切换 ERR_ABORTED 为 Vite dev 已知行为 |

E2E 已通过的关键路径包括：异步 Agent stream、Agent Eval/Agent Call、同步 Agent stream、附件状态、真实视觉模型图片读取、PDF OCR、非视觉模型 OCR fallback、Research Copilot、SubAgent 文件产物、Viewer 文件权限/删除/上传。第二轮完整 E2E 为 11 passed、0 skipped，此前第一轮失败仅因 worker watcher 崩溃后发送 SIGINT 导致 PDF run 被标记为 cancelled（见 R-10）。

## 七、本轮处理结论与后续顺序

1. C-01～C-06、T-01～T-05、R-10 已修复并完成分层回归。
2. R-01、R-02、R-03、R-05、R-06、R-07 已按最小方案处理；R-04 已明确为合法能力；R-08 已部分修复（Neo4j 噪声降噪）。
3. E-07 依赖漏洞升级完成，从 34 项降至 3 项剩余（nltk 无修复版本、setuptools 为构建时问题）。
4. R-08 的 MinIO 空状态噪声保留为非阻断可观测性技术债。
5. R-09 必须先用典型库和上限库完成耗时/内存/往返基线，再决定批处理或流式化范围。

## 八、全量修复后的复验清单

- [x] C-01～C-06 各自的验收项全部通过。
- [x] R-10 Redis runtime cache 与 worker watcher 修复后，第二轮完整 E2E 11 passed、0 skipped。
- [x] 后端单元测试全绿（1505 passed、1 skipped），skip 仅保留有书面理由的外部依赖场景（MinerU OCR 服务未启动）。
- [x] 后端集成测试（187 passed、4 skipped）不再出现跨事件循环错误，且没有因本地 fixture 缺失而无条件 skip。
- [x] 真实 E2E 的 Agent 同步/异步、Research Copilot、SubAgent 文件产物、附件、Viewer、PDF OCR 均通过。
- [x] 前端单元测试、ESLint、生产构建通过。
- [x] 全仓 `ruff check .` 与 `ruff format --check .` 全绿。
- [x] `uv pip check` 通过（305 个包兼容）；`pip-audit --local` 经依赖升级后从 34 项降至 3 项剩余（nltk 无修复版本、setuptools 为构建时问题，见 E-07）。
- [x] `pnpm audit --prod` 中 R-05 所列 advisory 已清除。
- [x] 默认 Compose 与 LITE_MODE 均可启动；从零 `down --remove-orphans` + `up -d --build` 重建成功，10 个服务全部启动且基础设施 healthy；LITE_MODE 在导入或执行科研恢复逻辑前直接返回。
- [x] API 日志中不存在测试哨兵敏感值，也没有连接泄漏或事件循环错误；从零重建后 worker-dev restart=0，无 watcher 崩溃，Redis 无 DNS 错误。
- [x] 真实用户主链路浏览器走查 8/8 通过（登录/ResearchCompass 首页/Agent 对话/附件上传/知识库入口/业务子页面/页面跳转）。
- [x] 修复过程只改与对应条目直接相关的代码、测试和文档，未顺便处理已排除项或大版本迁移。
