# 附：核实清单

> 标注每条结论的确认状态。**已读源码** = 本次直接读取该文件/函数确认；**⚠️ 待核实** = 未能从代码确认，列出原因与建议核查位置。

## 已读源码确认（强证据）

| 结论 | 证据位置 |
|------|----------|
| 项目定位：Yuxi 框架之上的科研领域层 | `AGENTS.md`、`ARCHITECTURE.md`（全文） |
| 框架栈：LangGraph v1 + Vue3 + FastAPI + LightRAG | `AGENTS.md` 开发准则段 |
| 科研接口全量在 `research_router.py`（1747 行） | `backend/server/routers/research_router.py`（已读头部 + 路由名） |
| 领域错误→HTTP 解耦 | `research_copilot_service.py` L40-52；`research_router.py` L30-56 |
| 学术分块保留 section_path/level/equations | `knowledge/chunking/ragflow_like/parsers/academic.py`（已读 `_chunk_document`/`_build_chunk_metadata`） |
| 混合检索三源 + Flashrank 重排 | `research_search_service.py` L137/L242/L275/L463/L570 |
| 综述聚类（embedding/section）+ 反幻觉校验 | `research_synthesis_service.py` L127/L635/L750/L885/L112 |
| 四阶段 LangGraph 工作流（structure/innovations/methodology/gaps） | `academic_paper_analysis_workflow.py` + `academic_paper_analysis_service.py`（已读结构） |
| 引用图谱双向边 + key 去重 merge | `academic_graph_service.py`（已读结构） |
| Agent 运行态接缝：slug + source 白名单 | `run_worker.py` L51/L62-70 |
| 27+ 科研工具 @tool 注册 | `toolkits/research/tools.py`（已读头部 + 输入模型） |
| 报告确定性拼装不调 LLM | `research_project_report_service.py`（已读说明）+ `MEMORY.md` 记录 |
| 前端对话优先 + 工作台检视 | `web/src/apis/research_api.js` + `views/research/`（已读 API 映射） |
| Docker Compose 多服务编排 | `docker-compose.yml`（已读） |

## ⚠️ 待核实（弱证据 / 推断）

| 待核实项 | 原因 | 建议核查位置 |
|----------|------|--------------|
| 向量库具体选型（pgvector / Milvus / Qdrant） | compose 未逐行确认，仅从 ARCHITECTURE 推断 | `docker-compose.yml` 全量 + `backend/package/yuxi/storage/` |
| 引用图谱后端是否含独立 Neo4j | 代码层写"Neo4j/Postgres 双向同步"，但 compose 未见 Neo4j 服务 | `docker-compose.yml`、`academic_graph_service.py` 连接配置 |
| `_search_legacy_papers` 外部 API 的 key/配额/限流 | 本次未读外部调用实现 | `research_search_service.py` L463 起 |
| `_cluster_chunks` 默认走 embedding 还是 section，k 如何定 | 未读 `run_synthesis` 的 options 默认分支 | `research_synthesis_service.py` `run_synthesis` 入参 |
| 前端对检索"部分失败 vs 全失败"的区分处理 | 未读前端错误处理组件 | `web/src/views/research/` 对应页面 |
| `sync_document` 引用解析实现（正则/GROBID/扫描件健壮性） | 未逐行读解析逻辑 | `academic_graph_service.py` `extract_citations` |
| 框架↔领域接缝的集成/e2e 测试是否存在 | `research_router.py` 见单测引用，未见科研主链路 e2e | `backend/test/integration`、`backend/test/e2e` |
| 重排模型是否可配置化（当前硬编码 MiniLM） | 仅见硬编码字符串 | `research_search_service.py` L570 上下文 |

## 方法声明

- 本次分析采用**方式 A（直接读取本地源码）**，未使用 DeepWiki 等外部分析器。
- 所有"已读源码确认"项均指向具体文件与行号/函数名。
- 未发现需要跨源（方式 C）交叉验证的显著结论冲突。
- 无编造内容；无法确认处一律标注 ⚠️ 待核实。
