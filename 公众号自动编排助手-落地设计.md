# 公众号自动编排助手 — 落地设计文档

> 本文档是「在 Yuxi 框架（L2 脚手架）上搭建公众号专属写作智能体（L3 应用）」的设计蓝本。
> 可直接作为新项目的起点文档，按需增删。

---

## 一、这是什么（一句话定位）

一个**公众号专属的写作 / 编排 / 结果处理智能体**：用户只需「上传范文到知识库 → 配置写作规范到技能 → 打开对话说一句『写一篇关于 X 的公众号』」，系统即按设定流程自动产出可溯源的公众号文章并保存，且每次运行都默认处在公众号语境中。

它不是一个通用聊天机器人，而是**只懂公众号这件事**的垂直助手。

---

## 二、为什么不直接用通用 AI 聊天工具

| 维度 | 通用 AI 聊天（Codex / OpenCode / 网页对话） | 本系统 |
|------|-------------------------------------------|--------|
| 背景交代 | 每次重新解释要求、重新贴范文 | 要求冻结为 Skill、范文冻结为知识库，一次配置反复用 |
| 提示词稳定性 | 易漂移，每次说法不同结果不同 | 规范固化，输出风格一致 |
| 产物沉淀 | 写完即丢，下次重来 | 成文落盘，参考来源可追溯 |
| 可审计性 | 无 | 每次成文参考了哪篇范文、哪条规范，均有记录 |
| 语境 | 什么都能聊，需每次带入公众号背景 | 默认就是公众号语境，开箱即写 |
| 演进 | 功能固定，新需求得重新手工prompt | 新能力以新工具/@tool 接入，系统持续长能力 |

**核心差别**：通用 AI 是「术业有专攻前的通用工具」，本系统是「把公众号这件事的方法论沉淀成可复用、可证明、可演进的系统」。

---

## 三、核心模式：L2 脚手架 + L3 填空

框架（Yuxi）只提供 3 个**持久化扩展槽**，任何领域应用都是往里填空：

| 扩展槽 | 框架原语 | 在公众号场景里填什么 |
|--------|---------|---------------------|
| ① 知识库 KB | 文档上传 / CRUD（原生） | 范文、案例、品牌调性资料、历史爆款 |
| ② 技能 Skill | prompt + 约束包（原生配置） | 写作规范、结构模板、语气要求、禁区、流程步骤 |
| ③ 工具 Tool | `@tool` 函数（写一处即全局注册） | 保存成文档、联网搜索热点、格式转换等动作 |
| 绑一个 agent | 原生运行时 | `wechat-copilot`：绑定上面的 KB + Skill + Tool |

**你做的所有事 = 填空，不是造轮子。** 框架已提供文档管理、技能管理、工具注册、对话运行时、沙箱、审计。

---

## 四、需求 → 框架原语映射（落地清单）

| 需求 | 对应原语 | 是否需自己写代码 | 说明 |
|------|---------|----------------|------|
| 上传范文 / 案例 | 知识库文档 | 否，直接上传 | 放进 KB，对话时可被检索引用 |
| 写作要求 / 规范 / 流程 | Skill 或 agent system prompt | 否，配置即可 | 语气、结构、字数、禁区、成文步骤 |
| 读要求 + 读范文 + 成文 | 对话 agent（原生） | 否 | 框架原生运行时驱动 |
| 联网搜索热点 / 素材 | 原生 Tavily 工具 或 复用 buildin | 否 | 框架已带网页搜索 |
| 保存成文档 / 落盘 | buildin workspace/artifact 工具 或 1 个通用 save 工具 | 基本否，最多 1 个 | 「按公众号格式保存」可用一个轻量 `@tool` |
| 参考来源溯源 | `tool_calls` 审计 + KB 引用 | 否，框架自带 | 每篇成文可回溯参考了哪些范文/规范 |
| 反复迭代更新 | KB / Skill 更新（原生 CRUD） | 否 | 改范文、改规范即生效，无需改代码 |

> 结论：**MVP 阶段大概率 0 个新工具**，完全靠原生原语填空即可跑通。

---

## 五、MVP 落地步骤（最小可用）

1. **新建 agent**：在 Yuxi 注册 `wechat-copilot`，绑定知识库 + 技能（等同于 ResearchCompass 注册 `research-copilot` 的同一机制）。
2. **上传范文**：往知识库传 3–5 篇高质量范文 + 品牌资料。
3. **写 Skill**：定义「公众号写作规范」——目标读者、语气、标题套路、正文结构（开头钩子/干货/金句/结尾引导）、字数区间、禁用词。
4. **复用工具**：优先复用框架 buildin 的保存/搜索工具；若需「按公众号格式落盘」，写 1 个轻量 `@tool`。
5. **跑通对话**：说「写一篇关于 X 的公众号」→ 系统自动读取规范 + 检索范文 + 成文 + 保存 + 标注参考来源。

---

## 六、演进路径：当通用能力不够时

现在觉得「很简单、用个网页对话就行」，是因为当前需求落在框架原生原语内。但公众号写作未来会衍生出框架原生覆盖不了的动作，那时就需要新增 `@tool`——框架的 Tool 槽位正是为此预留：

- 自动配图 / 封面生成 → 新工具
- 多平台格式转换（公众号 / 知乎 / 头条） → 新工具
- 热点日历抓取与选题建议 → 新工具
- 历史数据复盘（哪类标题打开率高） → 新工具
- 合规审查 / 敏感词拦截 → 新工具

**每加一个工具，系统就长一项专属能力，且仍走同一套注册 / 执行 / 审计机制。** 这正是「做这个系统有意义」的根本原因：它从第一天起就为演进留好了接口，而不是每次需求来了都重新手写 prompt。

---

## 七、价值与痛点（对外讲清楚的话术）

- **痛点**：写公众号的人每次都要从头交代背景、贴范文、定规范，通用 AI 用完即丢、不可复用。
- **价值**：专属语境 + 规范固化 + 范文复用 + 产物可溯源 + 能力可演进。
- **一句话**：把「写公众号的方法论」从「每次口头交代」变成「一套可运行、可审计、可生长的系统」。

---

## 附：与 ResearchCompass 的对照（验证模式可复用）

| 项 | ResearchCompass（科研） | 公众号助手（写作） |
|----|------------------------|-------------------|
| KB 填什么 | 论文 PDF / 文献 | 范文 / 品牌资料 |
| Skill 填什么 | 科研流程规范 | 写作规范 / 结构模板 |
| 自写工具量 | 27 个 `@tool` + service 层（跨工具领域流程） | 0–1 个（几乎无跨工具编排） |
| agent | `research-copilot` | `wechat-copilot` |
| 复杂度来源 | 领域本身跨工具 | 领域基本单链路，更简单 |

两者共用同一套框架机制，只是填空内容不同——**印证「L2 脚手架 + L3 填空」是可复制的元模式**。
