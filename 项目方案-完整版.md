# 科研罗盘 ResearchCompass — 项目方案（完整版）

> **定位**：AI 驱动的学术文献智能分析平台
> **赛事**：第八届中国研究生人工智能创新大赛 · 开放命题 · 方向三「AI+X 应用」
> **脚手架**：Yuxi（FastAPI + LangGraph + Vue3，Agent-first 平台底座）
> **规划版本**：完整版（全功能目标，执行中按风险动态降级，见《规划与决策.md》）
> **最后更新**：2026-07-21

---

## 一、项目定位

| 项 | 内容 |
|----|------|
| 中文名 | 科研罗盘 |
| 英文名 | ResearchCompass |
| 一句话主张 | 用「知识图谱 + 多 Agent」把科研文献从「能搜到」变成「能读懂、能对比、能发现方向」 |
| 参赛赛道 | 开放命题方向三：面向科学、工程和社会科学的「AI+X」应用 |
| 类别倾向 | 技术创新类（可运行原型 + 创新算法增量） |
| 目标奖项 | 二等奖保底，冲击一等奖 |

**解决的真问题**：① 文献检索不精准，看不到论文间关系；② 论文分析耗时长，易漏核心贡献；③ 研究趋势难把握，不知方向如何演化、哪里有空白。

---

## 二、核心创新点（为什么不是套壳）

> 用框架不 low，只套壳才 low。以下三点是 Yuxi 之上**额外做的工作**，是评审区分度来源。

### 创新点 1：知识图谱 + RAG 混合检索
- **传统 RAG 问题**：只能召回相似片段，缺论文间「关系」理解，无法回答「谁引用了谁」「这个方向谁在做」。
- **我们的方案**：向量检索（Milvus 找语义相关）→ 图谱扩展（Neo4j 找引用/合作/主题关联）→ 图谱推理（发现隐含关联 A→B→C）→ 重排序（向量相似度 + 图谱中心性加权）→ 生成带引用来源 + 关系图谱的回答。
- **实现**：复用 `knowledge/graphs/`，扩展论文引用关系抽取（优先 Semantic Scholar API）；检索层叠加 BM25 + Cross-Encoder 重排。

### 创新点 2：多 Agent 协作的论文分析流水线
- **传统做法**：单模型一口气总结，易幻觉、漏点、无法对比。
- **我们的方案**（LangGraph 编排，每 Agent 专注子任务）：
```
论文 PDF → Agent1 结构化提取 → Agent2 创新点识别
        → Agent3 方法论分析 → Agent4 研究空白发现 → 综合报告
```
- **实现**：继承 `BaseAgent`，独立调试 Prompt 后 LangGraph 串联；Agent4 依赖图谱，不可用时降级为「基于向量检索的已有工作对比」。

### 创新点 3：学术论文语义分块策略
- **传统分块**：固定长度硬切，破坏公式/表格/章节语义完整性。
- **我们的方案**：按章节结构分块（Abstract/Intro/Method/Exp/Conclusion 独立）+ 每块附带论文元信息 + 公式表格单独处理。
- **实现**：扩展 `knowledge/chunking/`，新增 `academic_chunker.py`，利用 MinerU 结构化解析。

---

## 三、最终效果演示（评审 / 用户视角）

### 主界面
```
┌───────────────────────────────────────────────────────────┐
│  科研罗盘 ResearchCompass        [我的知识库] [个人中心]   │
├───────────────────────────────────────────────────────────┤
│  🔍 文献检索   📊 论文分析   📈 趋势分析   🕸 关系图谱  │
├───────────────────────────────────────────────────────────┤
│   用户：帮我找 Transformer 在医学图像上的应用，并分析趋势   │
│   AI：找到 23 篇相关论文，以下核心 5 篇 + 关系图谱：   │
│       1. XXX (2021) — 核心贡献：xxx   [查看原文↗]        │
│       2. YYY (2023) — 引用了 1，方法演进：xxx           │
│   [论文关系图谱可视化区]                                    │
└───────────────────────────────────────────────────────────┘
```

### 场景 1：智能文献检索（混合检索 + 图谱）
自然语言提问 → 查询改写 → 向量 + 图谱双路召回 → 重排 → 论文卡片（标题/作者/年份/引用数/核心贡献）+ 右侧关系图谱（可点节点下钻）。

### 场景 2：论文深度分析（多 Agent 流水线）
拖拽上传 PDF → 进度条依次走过 4 个 Agent → 输出交互式报告：📄 论文结构（章节导航，点击跳原文）· 💡 核心创新点（3–5 个，标注与已有工作差异）· 🔬 方法论（技术路线/实验设计）· 🎯 研究空白。

### 场景 3：研究趋势分析（图谱查询 + 可视化）
输入主题 + 时间窗 → 按年份统计 → 关键词演进 → 新兴方向识别。图表：论文量折线、关键词云、研究网络力导向图、新兴方向卡片。

### 场景 4：引用溯源
回答中任意论断可点击 → 跳转到原文具体段落（PDF 高亮），每个结论有据可查，杜绝幻觉。

---

## 四、系统功能清单（完整版 · 全功能）

| # | 功能模块 | 子功能 | 优先级 | 创新层级 |
|---|---------|--------|--------|---------|
| 1 | 基础 RAG 检索 | 向量检索 + 关键词检索 + 重排 | P0 | 第 1 层 |
| 2 | 学术语义分块 | 按章节分块 + 元信息附加 + 公式表格隔离 | P0 | 第 2 层 |
| 3 | 知识图谱构建 | Neo4j 论文节点 + 引用/合作/主题边 | P1（最大亮点） | 第 2 层 |
| 4 | 图谱混合检索 | 向量召回 + 图谱扩展 + 推理 + 重排 | P1 | 第 2 层 |
| 5 | 多 Agent 分析 | 结构化提取 / 创新点 / 方法论 / 空白发现 | P1 | 第 2 层 |
| 6 | 引用溯源 | 论断点击 → 原文段落高亮 | P1 | 第 2 层 |
| 7 | 研究趋势分析 | 数量趋势 / 关键词演进 / 新兴方向 | P2（可选） | 第 2 层 |
| 8 | 关系图谱可视化 | 力导向图 + 节点下钻 | P2 | 第 2 层 |
| 9 | 用户系统 | 多租户 / 知识库隔离 / 历史记录 | P3 | 演示加分 |

---

## 五、技术架构

```
┌─────────────────────────────────────────────┐
│  前端层 Vue 3（改造 Yuxi 现有视图）          │
│  AgentView  DataBaseView  Dashboard         │
│  + 新增：PaperAnalysis / GraphView / Trend  │
└──────────────────┬──────────────────────────┘
                    ↕ REST API
┌─────────────────────────────────────────────┐
│  后端层 FastAPI + LangGraph（Yuxi 底座）     │
│  agents/        knowledge/      services/    │
│  (多Agent编排)   (RAG+图谱)      (业务逻辑) │
└──────────────────┬──────────────────────────┘
                    ↕
┌─────────────────────────────────────────────┐
│  存储层：PostgreSQL  Redis  Milvus  Neo4j  MinIO │
└─────────────────────────────────────────────┘
```

**复用 Yuxi（不重复造轮子）**：RAG 检索管线、文档解析（MinerU）、Agent 基类与中间件、多租户框架、基础前端视图。
**新增 / 改造（创新所在）**：`academic_chunker.py`、图谱引用关系抽取器、4 个科研分析 Agent + LangGraph 串联、前端分析卡片/关系图谱/趋势图。

---

## 六、实现路线图（完整版）

> 真实起手日约 **2026-07-26**（安顿健康医学院后）。完整版全功能目标，按「图谱能否跑通」动态降级。

### 第 1 周（7/26–8/1）：环境验证 + 核心跑通
- [ ] 启动 Yuxi Docker 全栈（Milvus / Neo4j / Postgres / Redis / MinIO）
- [ ] 灌入 10–20 篇测试论文 PDF
- [ ] 验证基础 RAG 检索可用
- [ ] **关键决策点**：知识图谱能否自动构建 + 引用关系抽取是否准确
  - ✅ 跑通 → 走完整版 ／ ❌ 跑不通 → 图谱降级为「向量 + BM25」，保留语义分块 + 多 Agent

### 第 2 周（8/2–8/8）：检索增强 + 语义分块
- [ ] 学术语义分块策略
- [ ] 混合检索（向量 + BM25 + 重排）
- [ ] 图谱可用时：开发图谱混合检索；集成 Semantic Scholar API 补全引用关系

### 第 3 周（8/9–8/15）：多 Agent 分析流水线
- [ ] Agent1 结构化提取 → Agent2 创新点识别 → Agent3 方法论 → Agent4 研究空白（依赖图谱，不可用则降级）
- [ ] 串联 LangGraph + 前端分析报告页

### 第 4 周（8/16–8/22）：趋势分析 + 前端打磨
- [ ] 趋势分析 Agent（数量 / 关键词 / 新兴方向）
- [ ] 关系图谱可视化（vue-flow / d3）
- [ ] 引用溯源交互 + 前端美化 + 演示数据准备

### 第 5 周（8/23–8/29）：实验验证 + 文档
- [ ] **消融实验**：纯向量 vs 向量+图谱；通用分块 vs 学术分块；单 Agent vs 多 Agent
- [ ] **量化对比**：与通用 RAG 框架、直接问 LLM 在领域测试集上的指标对比
- [ ] **用户评测**：3–5 名研究生试用 + 问卷
- [ ] 录制演示视频（5–10 分钟）+ 按官方模板写项目文档

### 最后冲刺（8/30–9/1）：提交
- [ ] 作品简介（300 字）／整理代码 + README
- [ ] **8/25 前完成官网报名** ／ **9/1 前提交全部材料**（简介 + 文档 + 视频）

---

## 七、拿奖逻辑（三层竞争力）

| 层级 | 做到什么 | 对应奖项 |
|------|---------|---------|
| 第 1 层：能用 | 部署框架 + 灌数据 + 跑通 | 三等奖 |
| 第 2 层：好用 | 语义分块 + 混合检索 + 多 Agent + 引用溯源 | 二等奖 |
| 第 3 层：能证 | 消融实验 + 量化对比 + 用户评测 | 一等奖 |

**完整版目标**：稳在第 2 层，靠第 3 层实验证据冲一等奖。

---

## 八、风险与降级预案

| 风险 | 表现 | 预案 |
|------|------|------|
| 知识图谱构建失败 | Neo4j 配置 / 引用关系抽取不准 | 降级「向量 + BM25」，保留语义分块 + 多 Agent |
| 多 Agent 调试超时 | Prompt 调不动 / 串联 bug | 先单 Agent 验证，不够只留 Agent1+2 |
| 前端开发超时 | 图谱可视化做不完 | 先用 Yuxi 原界面 + 最小新增，功能优先 |
| 新环境机器受限 | Docker 栈跑不动 / 无 GPU | 改用轻量栈（rag-web-ui）或云端部署 |
| 时间不够全功能 | 37 天偏紧 | 趋势分析 / 关系图谱降为「未来工作」，保检索 + 分析 + 溯源 |

---

## 九、交付物清单

**初赛（线上）**：作品简介（300 字，无模板）／项目文档（官方模板，禁含学校·学院·导师信息）／演示视频（禁含上述信息）／可选辅佐材料（技术可行性报告 / 用户评测报告）。
**决赛（现场路演 + 答辩，济南 / 山东大学）**：PPT 汇报／系统现场演示／评委问答（重点：创新点讲清、消融实验数据过硬）。

---

## 十、作品简介（300 字版 · 报名用）

**科研罗盘（ResearchCompass）** 是一个基于「知识图谱 + RAG」的学术文献智能分析平台。针对科研人员「文献检索不精准、论文分析耗时长、研究趋势难把握」的痛点，我们创新性地将**向量检索**与**知识图谱推理**结合，实现：(1) **混合检索**——向量召回相关论文、图谱扩展引用关系，检索准确率显著提升；(2) **多 Agent 协作**——结构化提取→创新点识别→方法论分析→研究空白发现，自动生成论文分析报告；(3) **学术语义分块**——按章节语义分块、保留公式表格完整性，召回精度提升；(4) **研究趋势可视化**——基于图谱分析论文演进、关键词迁移与新兴方向。技术栈：LangGraph 多智能体编排 + Neo4j 知识图谱 + Milvus 向量库 + FastAPI + Vue3。通过消融实验证明图谱增强效果显著，是国内将知识图谱引入学术 RAG 的系统性尝试。

---

## 十一、关键技术要点（代码级）

**目录**：`D:\Develop\SourceCode\agent-projects\ResearchCompass\Yuxi\`

**学术语义分块（新增）**
```python
# backend/package/yuxi/knowledge/chunking/academic_chunker.py
class AcademicChunker:
    def chunk(self, text, metadata):
        sections = self.identify_sections(text)      # 识别章节标题
        return [{"text": s.content, "metadata": {
            "section": s.name, "paper_title": metadata["title"],
            "authors": metadata["authors"]}} for s in sections]
```

**论文分析 Agent（新增，继承 BaseAgent）**
```python
# backend/package/yuxi/agents/buildin/paper_analysis/
class PaperAnalysisAgent(BaseAgent):
    async def extract_structure(self, paper_text): ...   # Agent1
    async def identify_innovations(self, structure): ...  # Agent2
```

**关键代码位置**
- 知识图谱：`backend/package/yuxi/knowledge/graphs/`（MilvusGraphService、Neo4j 封装、LLM 抽取器）
- Agent 体系：`backend/package/yuxi/agents/`（BaseAgent、middlewares、toolkits、buildin 示例）
- 文档解析：`backend/package/yuxi/knowledge/parser/`（MinerU / PaddleX / RapidOCR）
- 前端页面：`web/src/views/`（AgentView / DataBaseView / DashboardView）

**引用关系补全（优先方案）**
```python
import requests
def get_citations(paper_title):
    return requests.get("https://api.semanticscholar.org/graph/v1/paper/search",
                       params={"query": paper_title}).json()["citations"]
```

---

## 十二、给自己的话

> 完整版不是「全做了才交」，而是「以全功能为目标规划，执行中按风险动态降级」。
> 先验证 Yuxi 在新环境能跑——这一步决定走完整版还是降级版。
> 不绑老师，做出东西再说。框架不丢人，丢人的是只套壳不创新。

---

## 附录一：功能验收标准

| 功能 | 验收标准 | 目标指标 |
|------|---------|---------|
| 知识图谱混合检索 | 返回向量结果 + 图谱扩展的引用关系论文 + 「看了这篇还看了…」推荐 | 召回率较纯向量检索提升 ≥30% |
| 多 Agent 分析 | 上传 15–20 秒出报告，含结构化信息 / 3–5 创新点 / 方法论 / 研究空白 | 创新点准确率 ≥80%（人工评估） |
| 论文语义分块 | 按章节正确分块，每块带元信息 | 召回精度较传统分块提升 ≥25% |
| 论文关系图谱 | 节点大小=引用次数，可点击查看详情 | 可视化正确渲染 |
| 引用溯源 | 每条信息标来源，点击跳 PDF 原文并高亮 | 跳转 + 高亮准确 |

测试样例：查询「Multi-Head Attention 是什么」应召回 Method 章节而非 Abstract/Intro；上传「Attention is All You Need」应正确返回 Multi-Head Attention / Positional Encoding / 并行化等创新点。

## 附录二：竞争力评分细则（目标参考）

| 维度 | 分值 | 要点 |
|------|------|------|
| 技术创新 | 30 | 知识图谱+RAG 混合检索 / 学术语义分块 / 多 Agent 协作 |
| 实现质量 | 30 | 功能完整、演示流畅、前端可用、响应 5–15 秒 |
| 实验验证 | 20 | 消融实验 + 量化对比基线 + 用户评测 |
| 应用价值 | 20 | 解决真实痛点、可推广、有潜力 |

**风险概率（经验估计）**：知识图谱构建失败 ~20% ／ 时间不够 ~30% ／ 多 Agent 调试困难 ~40%。
> 注：「国内首创」「提升 40%/50%」「总分 85–95」等均为**目标/预测**，非实测，写进提交材料前须用真实实验数据替换。

## 附录三：前端图谱可视化代码示例

```vue
<!-- web/src/components/GraphView.vue（基于 @vue-flow/core） -->
<template>
  <div class="graph-container">
    <h3>论文引用关系图谱</h3>
    <VueFlow :nodes="nodes" :edges="edges" @node-click="handleNodeClick">
      <template #node-default="{ data }">
        <div class="paper-node">
          <h4>{{ data.title }}</h4>
          <p>{{ data.year }}</p>
          <span class="citations">{{ data.citations }}次引用</span>
        </div>
      </template>
    </VueFlow>
    <PaperDetail v-if="selectedPaper" :paper="selectedPaper" @close="selectedPaper = null" />
  </div>
</template>
<script setup>
import { ref, onMounted } from 'vue'
import { VueFlow } from '@vue-flow/core'
const nodes = ref([]), edges = ref([]), selectedPaper = ref(null)
onMounted(async () => {
  const data = await (await fetch('/api/graph/citations?paper_id=123')).json()
  nodes.value = data.nodes.map(n => ({ id: n.id, position: { x: n.x, y: n.y },
    data: { title: n.title, year: n.year, citations: n.citations } }))
  edges.value = data.edges.map(e => ({ id: e.id, source: e.source, target: e.target, label: '引用' }))
})
const handleNodeClick = (e) => { selectedPaper.value = e.node.data }
</script>
```

## 附录四：详细逐日排期（参考，日期未调整）

> 以下为另一版逐日细化，起手按原 7/22 排，**实际执行日期以《我的参赛档案.md》为准**（真实起手约 7/26）。

- **第 1 周**：Day1 启 Yuxi Docker 全栈并验服务；Day2–3 验知识图谱（Neo4j 实体/关系）；Day4–5 准备 50 篇论文 + Semantic Scholar 元数据；Day6–7 验向量/图谱/混合检索
- **第 2 周**：Day8–10 学术语义分块（academic_chunker.py）；Day11–14 Agent1 结构化提取
- **第 3 周**：Day15–17 图谱混合检索（hybrid_search_service.py）；Day18–21 Agent2 创新点识别
- **第 4 周**：Day22–24 Agent3 方法论；Day25–26 Agent4 研究空白；Day27–28 串联 4 Agent 流水线
- **第 5 周**：Day29–31 前端卡片（PaperAnalysisCard / CitationView）；Day32–34 图谱可视化（vue-flow）；Day35 前后端联调
- **第 6 周**：Day36–37 消融实验；Day38 用户评测；Day39 录视频；Day40 写文档+简介；Day41 终检+PPT；Day42 提交
